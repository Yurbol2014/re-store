//function addBinary(a, b) {
//  let sum = a + b;

//  let arr = [];                          //Реализуйте функцию, которая складывает два числа и возвращает их сумму в двоичном виде. Преобразование может быть выполнено до или после добавления.

//  Возвращаемое двоичное число должно быть строкой.
  

//  let i = 0;
//  for (; sum > 0.5; i++) {
//    sum = Math.floor(sum) / 2;

//    if (Number.isInteger(sum) === true) {
//      arr.push(0);
//    } else arr.push(1);

//    console.log(sum);
//  }
//  let res = arr.reverse().join("");
//  return res;
//}

//addBinary(1, 1);
